from pathlib import Path

# Define the directories
DATA_DIR = Path('..') / "mlops-nba" / "data"
MODEL_DIR = Path('..') / "mlops-nba" / "model"

# Define the path to the version file
VERSION_FILE = Path('..') / "mlops-nba" / "version.txt"


